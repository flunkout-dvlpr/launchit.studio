# Copy this file to .env and fill in your real Google Analytics 4
# Measurement ID (Admin > Data Streams > your web stream, looks like
# G-XXXXXXXXXX). Left unset, analytics just silently no-ops — safe to
# leave out entirely for local dev.
VITE_GA_MEASUREMENT_ID=
